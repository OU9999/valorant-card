## 프로젝트 개요

- FC Ultimate Team 카드 구조와 Valorant 전술 디자인을 결합한 플레이어 카드 생성 서비스
- `pnpm` workspace 기반 모노레포
- Web 패키지 → `valorant-card-web`
- 이미지 생성 CLI → `nano-banana-cli`

## 상시 규칙

- 패키지 매니저는 `pnpm`만 사용
- 코드 작성·수정 전 `valorant-card-rules/code-rule/code-rule.md` 필독
- `docs/` 문서 작성·수정 시 `docs/docs-rule.md` 필독
- 디자인·UI·스타일링 작업 시 `valorant-card-web/DESIGN.md` 필독
- `AGENTS.md`와 `CLAUDE.md` 내용 drift 금지

## Git

- 커밋 시 `commit` 스킬 사용, 직접 `git commit` 실행 금지
- PR 생성 시 `pr` 스킬 사용, 직접 `gh pr create` 실행 금지
